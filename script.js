function exercise(){
    var totalNum = 0;
    var numbers=[10, 20, 30, 40, 50];

    for(var i=0; i < numbers.length; i++){
        var totalNum = totalNum + numbers[i];
    }
    
    console.log(totalNum);
}

exercise();

